#include <stdio.h>
#include <stdlib.h>



int main(){
	
	int row;
	int col;
	int cc;
	//gets the input from the user
	printf("Inputs: ");
	if(!scanf("%d %d", &row, &col)){
		printf("Please enter Numbers only");
		exit(1);
	}
	
	scanf("%d", &cc);
	printf("%d\n", cc);
	
	if(row>0){
		//printf(
	}
	
}